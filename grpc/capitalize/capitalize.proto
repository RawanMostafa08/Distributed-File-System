syntax = "proto3";

option go_package = "Distributed-File-System/grpc/capitalize";

package capitalize;

message TextRequest {
    string text = 1;
}

message TextResponse {
    string capitalized_text = 1;
}

service TextService {
    rpc Capitalize(TextRequest) returns (TextResponse);
}
